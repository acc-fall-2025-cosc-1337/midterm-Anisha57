#include "question2.h"  // Include the header file

// Function to convert Celsius to Fahrenheit
double get_fahrenheit(double c) {
    return (9.0 / 5.0) * c + 32;  // Fahrenheit formula: F = (9/5)*C + 32
}
